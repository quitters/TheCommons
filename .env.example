## OpenAI key used to generate new canvas code from a prompt.
## Without this set, the server runs entirely on its small built-in sketch
## library, so the app is fully demoable with zero configuration.
OPENAI_API_KEY=
MODEL=gpt-5.6-sol

PORT=4173
